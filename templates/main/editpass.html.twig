{% extends 'base.html.twig' %}

{% block body %}
    <div class="grix xs4 container">
        <div class=" col-xs4">
            {% for messsage in app.flashes('error') %}
            <div class=" col-xs4">
                <div class="p-3 my-2 rounded-2 error">{{ message }}</div>
            </div>
            {% endfor %}
            <h1>Modification du mot de passe de :{{ app.user.prenom }} {{ app.user.nom }}</h1>
                <form method="post">
                    <div class="form-field">
                        <label for="pass">Mot de passe</label>
                        <input type="password" id="pass" name="pass" class="form-control" placeholder="">
                    </div>
                    <div class="form-field">
                        <label for="pass2">Confirmer le mot de passe</label>
                        <input type="password" id="pass2" name="pass2" class="form-control">
                    </div>
                    <button class="btn blue">Valider</button>
                </form>
        </div>
    </div>



    {#<div id="profile_img">
       {% if app.user.picture %}
            <img src="{{ asset('uploads/profiles/'~user.picture) }}" alt="{{ user.prenom }} {{ user.nom }}">
        {% else %}
            <img src="{{ asset('img/image-profile-default.jpg') }}" alt="default image">
        {% endif %}
    </div>


    {% if app.user == user %}
        <a href="{{ path('user_edit') }}">Modifié le profil</a><br>
        <a href="{{ path('app.user_upload') }}">Modifié l'image</a><br>
        <a href="{{ path('user_edit_password') }}">Modifié le mot de passe</a>
    {% endif %} #}
{% endblock %}

{% block title %}Profil de {{ app.user.prenom}} {{ app.user.nom }}{% endblock %}