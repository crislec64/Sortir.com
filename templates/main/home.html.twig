<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=yes">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>HOME</title>
</head>
<header>

    {% include ('inc/nav.html.twig')%}

</header>
<body>
{% block body %}
<h2>Accueil</h2>
<table>
    <tr>
        <th>Nom de la sortie</th>
        <th>Date de la sortie</th>
        <th>Clôture</th>
        <th>inscrit/places</th>
        <th>Organisateur</th>
    </tr>
{% for sortie in sorties %}
    <tr>
        <td>{{ sortie.nom }}</td>
        <td>{{ sortie.dateHeureDebut |date('d-m-y H:i') }}</td>
        <td>{{ sortie.dateLimiteInscription |date('d-m-y') }}</td>
        <td>{{ sortie.nbInscriptionsMax }}</td>
{#        <td>{{ sortie.etat }}</td>#}
        <td>{{ sortie.organisateur }}</td>
        <td> <button>
                <a href="{{path('sortie_details', {'id':sortie.id}) }}" title="Afficher">Afficher</a>
            </button>
        </td>
        <td>
            <button>
                <a href="{{path('editSortie', {'id':sortie.id}) }}" title="Modifier">Modifier</a>
            </button>
        </td>
        <td>
            <button>
                <a href="{{path('sortie_delete', {'id':sortie.id}) }}" title="Supprimer">Supprimer</a>
            </button>
        </td>
    </tr>

    {% endfor %}
</table>
    {% endblock %}
</body>
</html>