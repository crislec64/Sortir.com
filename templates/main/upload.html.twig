{% extends 'base.html.twig' %}

{% block body %}
    <h1>Upload de photo de profil</h1>

    {{ form(form) }}

    <a href="{{ path('user_profile', {id: app.user.id}) }}" class="btn btn-danger">Passer cette étape</a>
{% endblock %}

{% block title %}Upload image de profil{% endblock %}